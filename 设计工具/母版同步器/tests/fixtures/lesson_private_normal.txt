# 第二部分 · 已沉淀教训(append-only,带阶段来源)

- **L1 · 符号方向(阶段1)**:`days_from_today` 约定"正数=已过去",与"距交付(剩余天数)"方向相反。
- **L2 · schema 留关联(阶段2)**:`qc_issue` 漏了与 `scan_run` 的关联,导致"只看最近一次扫描"只能靠时间戳硬凑。
- **L3 · 测试舒适区盲点(阶段2)**:CodeX 测了 `latest_run_only=False`、绕开了有问题的 `=True`。**带开关的接口,强制要求两条分支都测**。
